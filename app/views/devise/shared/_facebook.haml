- if devise_mapping.omniauthable?
  - resource_class.omniauth_providers.each do |provider|
    = link_to " Entre com #{provider.to_s.titleize}", omniauth_authorize_path(resource_name, provider), class: "fa fa-#{provider}-square fa-2x"
    %br/
